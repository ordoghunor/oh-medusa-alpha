## Storefront

This is the Medusa storefront app in this monorepo.

## Local development

```bash
npm run dev
```

By default, it runs on `http://localhost:8000`.

Set `NEXT_PUBLIC_MEDUSA_BACKEND_URL` to point to the backend API if needed.

## Build

```bash
npm run build
```
